# EZLove 易挂念 — 部署落地分析

> 本文档分析 EZLove 项目在真实环境部署的就绪程度，为决策者提供可执行的落地路线图。

---

## 部署就绪度总览

| 状态 | 模块 | 说明 |
|:---:|------|------|
| 🟢 可直接部署 | 社区管理后台、后端 API、Docker Compose 基础设施 | 代码完整，一键启动 |
| 🟡 需配置后部署 | 微信小程序发布、AI 功能、微信模板消息 | 代码完整，需外部账号/密钥 |
| 🔴 暂不可落地 | 核心检测机制验证、风险评分校准、社区对接、志愿者闭环 | 依赖真实用户和业务场景 |

---

## 可直接部署的模块（Green — Ready）

### 1. 社区管理后台（admin-frontend）

**技术栈：** Vue 3 + Element Plus + Tailwind CSS + Vue Router + Pinia

**已实现的完整页面：**

| 页面 | 功能 | 路由 |
|------|------|------|
| 数据看板 | 老人状态总览、关键指标、趋势图表 | `/dashboard` |
| 老人档案 | 档案管理、详情查看、风险等级标注 | `/elders` |
| 食堂管理 | 就餐记录、菜单管理、文本导入解析 | `/canteen` |
| 事件中心 | 社区活动创建、参与记录统计 | `/events` |
| 志愿者管理 | 志愿者列表、任务分配、积分管理 | `/volunteers` |
| AI 助手 | 智能对话、数据分析辅助 | `/ai-assistant` |
| 数据导出 | Excel/CSV 导出、报表生成 | `/export` |
| 登录页 | 账号密码认证、JWT 令牌管理 | `/login` |
| 系统设置 | 社区信息配置、权限管理 | `/settings` |

**部署方式：**

```bash
# 开发模式
cd admin-frontend
npm install
npm run dev  # localhost:5174

# 生产构建
npm run build  # 输出到 dist/，可部署到 Nginx/CDN
```

**就绪理由：**
- 所有页面 UI 完整，交互逻辑闭环
- API 请求层封装完毕，含 access/refresh token 自动刷新
- 路由守卫（JWT auth guard）已实现
- 设计系统（terracotta 陶赤主色系）统一，Tailwind 配置完备

---

### 2. 后端 API（backend）

**技术栈：** FastAPI + PostgreSQL（asyncpg）+ Redis + Alembic + APScheduler

**模块清单：**

| 层级 | 数量 | 说明 |
|------|:---:|------|
| API 路由模块 | 17 | 认证、用户、绑定、牵挂、告警、AI、社区、食堂、活动、志愿者等 |
| 服务层 | 19 | 完整业务逻辑，API 层与数据层解耦 |
| ORM 模型 | 16 | 覆盖所有业务实体，关系映射完整 |
| 数据库迁移 | 7 | Alembic 异步迁移链，可一键升级到最新 |
| 定时任务 | 6 | APScheduler 调度，含未读检测、告警生成、数据清理 |

**核心业务流已打通：**

```
子女发送牵挂 → care_moments 表
      ↓
老人查看内容 → view_events 表（自动记录 openid + 时间 + 时长）
      ↓
定时检测任务 → 未读超时判定 → alerts 表 → 通知子女
```

**部署方式：**

```bash
# 1. 配置环境变量
cp backend/.env.example backend/.env
# 编辑 .env，填入数据库和 Redis 连接信息

# 2. 启动服务
cd backend
pip install -r requirements.txt
alembic upgrade head       # 执行数据库迁移
uvicorn app.main:app --host 0.0.0.0 --port 8001

# 3. 初始化数据
python seed_community.py   # 创建社区 + 管理员账号
python seed_elders.py      # 导入老人模拟数据

# 4. 健康检查
curl http://localhost:8001/health
```

**就绪理由：**
- 四层架构（model → schema → service → api）每域完整
- 异步全栈，性能满足生产要求
- 认证体系双轨运行（微信登录 + 管理后台登录），互不干扰
- 错误处理、参数校验、分页查询等基础设施齐备

---

### 3. Docker Compose 基础设施

**服务编排：**

```yaml
services:
  db:
    image: postgres:16
    port: 5433        # 避开其他项目的 5432
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    port: 6380        # 避开其他项目的 6379

  backend:
    build: ./backend
    port: 8001
    depends_on: [db, redis]

  admin-frontend:
    build: ./admin-frontend
    port: 80          # Nginx 托管静态文件
```

**一键启动：**

```bash
# 仅数据库和缓存（开发时使用）
docker-compose up -d db redis

# 全部服务（生产部署）
docker-compose up -d

# 查看状态
docker-compose ps
docker-compose logs -f backend
```

**就绪理由：**
- 四个服务定义清晰，依赖关系明确
- 端口规划合理（PG=5433, Redis=6380, Backend=8001），避免与同机其他项目冲突
- 数据卷持久化配置完毕，数据库不会因容器重启丢失

---

## 需要配置后部署的模块（Yellow — Needs Config）

### 1. 微信小程序发布

**现状：** 前端代码 100% 完整，16 个页面全部实现。

| 页面域 | 包含页面 | 状态 |
|--------|----------|:---:|
| 登录 | 微信授权登录、开发模式登录 | ✅ |
| 发送牵挂 | 文字/图片/语音编辑、AI 建议、海报预览 | ✅ |
| 老人查看 | 大字版内容展示、已读状态上报 | ✅ |
| 绑定关系 | 老人绑定、子女绑定 | ✅ |
| 告警中心 | 告警列表、详情、处理 | ✅ |
| 个人中心 | 用户信息、设置、家庭成员管理 | ✅ |

**上线所需步骤：**

```
步骤 1：注册微信小程序账号
  └─ 访问 https://mp.weixin.qq.com 注册
  └─ 选择"企业"或"个人"主体
  └─ 获取 AppID 和 AppSecret

步骤 2：配置后端微信参数
  └─ 在 backend/.env 中设置：
     WX_APPID=your-appid-here
     WX_SECRET=your-appsecret-here

步骤 3：配置服务器域名
  └─ 微信公众平台 → 开发管理 → 服务器域名
  └─ request 合法域名：https://your-domain.com
  └─ uploadFile 合法域名：https://your-domain.com

步骤 4：构建并上传
  cd frontend
  npm run build:mp-weixin
  └─ 使用微信开发者工具打开 dist/build/mp-weixin 目录
  └─ 上传代码 → 提交审核 → 发布

步骤 5：小程序类目审核
  └─ 选择合适的小程序类目（生活服务/社区服务）
  └─ 如涉及老人健康数据，可能需要医疗相关资质
```

**注意事项：**
- 小程序审核通常需要 1-3 个工作日
- 首次发布建议使用"体验版"进行内部测试
- 小程序类目选择直接影响审核通过率

---

### 2. AI 功能

**涉及的 AI 调用点：**

| 功能 | 调用位置 | 用途 | 不配置时行为 |
|------|----------|------|-------------|
| 食堂文本解析 | `llm_parser.py` | 将文字菜单解析为结构化就餐记录 | 退回手动录入模式 |
| 牵挂文案建议 | `ai service` | 为子女生成温馨的牵挂内容建议 | 隐藏"AI 建议"按钮 |
| 风险评分 AI 分析 | `risk_scoring.py` | 智能分析老人风险趋势 | 仅使用规则引擎评分 |
| 小溪智能助手 | `ai assistant` | 社区工作人员的智能问答 | 助手入口不可用 |

**配置方式：**

```bash
# backend/.env

# Anthropic Claude API（食堂解析、文案建议）
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# 通用 LLM 接口（如使用兼容 OpenAI 格式的服务）
LLM_API_KEY=your-llm-api-key-here
LLM_API_BASE=https://your-llm-endpoint.com/v1
LLM_MODEL=your-model-name
```

**降级策略：**
所有 AI 功能均实现了优雅降级——不配置 API Key 时，相关入口自动隐藏或切换到手动模式，不会影响核心业务流程的正常运行。

---

### 3. 微信模板消息

**涉及的消息模板：**

| 场景 | 环境变量 | 触发条件 |
|------|----------|----------|
| 未读提醒 | `WECHAT_UNREAD_TEMPLATE_ID` | 老人超过设定时间未查看内容 |
| 告警通知 | `WECHAT_ALERT_TEMPLATE_ID` | 系统检测到异常风险 |

**配置方式：**

```bash
# backend/.env

# 在微信公众平台申请模板消息后获取模板 ID
WECHAT_UNREAD_TEMPLATE_ID=your-template-id-here
WECHAT_ALERT_TEMPLATE_ID=your-template-id-here
```

**申请流程：**
1. 登录微信公众平台 → 功能 → 模板消息
2. 选择或申请消息模板（需包含：标题、内容摘要、时间等字段）
3. 审核通过后获取模板 ID
4. 填入 `.env` 配置文件

**不配置时行为：** 定时任务正常运行，告警正常生成并记录到数据库，但不会通过微信推送通知。子女需主动打开小程序查看告警信息。

---

## 暂不可落地的模块（Red — Not Yet Feasible）

### 1. "已读即在线"核心检测机制

**问题：** 这是 EZLove 的产品骨架——通过老人查看牵挂内容的行为来判断其是否安全在线。该机制的有效性完全依赖于：

- 老人实际打开小程序查看子女发送的内容
- 老人查看行为具有足够频率和规律性
- view_events 数据能真实反映老人的日常活动状态

**现状：**
- 技术层面完全就绪：view_events 表、自动记录逻辑、定时检测任务均已实现
- 但系统中目前只有模拟数据，没有真实的老人在使用

**验证所需：**
- 至少 10-20 位真实老人用户持续使用 2-4 周
- 收集查看频率、查看时长、未读率等关键指标
- 根据真实数据调整未读超时阈值（当前默认值为配置参数）

---

### 2. 风险评分算法验证

**现状：** `risk_scoring.py` 实现了完整的多因子评分引擎：

| 维度 | 权重 | 数据来源 |
|------|:---:|----------|
| 查看活跃度 | 30% | view_events 表 |
| 食堂就餐规律 | 20% | canteen_records 表 |
| 社区活动参与 | 15% | event_participations 表 |
| 告警历史 | 20% | alerts 表 |
| 社交连接度 | 15% | relations + interactions |

**问题：**
- 权重系数基于经验设定，未经真实数据校准
- 风险阈值（低风险/中风险/高风险分界点）为默认值
- 评分分布是否合理，需要大量真实样本验证

**校准所需：**
- 至少 100+ 位老人的 3 个月以上活动数据
- 与实际社区工作者反馈对照，验证评分准确性
- 可能需要引入机器学习方法动态调整权重

---

### 3. 社区实际对接

**问题：** 管理后台功能完备，但需要与真实社区组织对接：

| 对接事项 | 具体内容 | 复杂度 |
|----------|----------|:---:|
| 数据导入 | 将社区现有老人档案导入系统 | 中 |
| 人员培训 | 社区工作人员学习使用管理后台 | 低 |
| 流程融合 | 将系统嵌入社区日常工作流程 | 高 |
| 权限配置 | 按实际组织架构配置账号和权限 | 低 |
| 数据合规 | 确保老人个人信息收集合规 | 高 |

**所需条件：**
- 找到 1-2 个愿意试点的社区
- 社区有专职或兼职工作人员能操作管理后台
- 获得老人及其家属的数据使用授权

---

### 4. 志愿者系统闭环

**现状：** 志愿者管理模块代码完整，包括：

- 志愿者注册与认证
- 服务任务发布与领取
- 服务时长记录与积分累计
- 积分兑换与表彰机制

**问题：**
- 需要真实的志愿者组织参与运营
- 积分兑换需要实际的激励资源（商家赞助、社区荣誉等）
- 任务分配需要与社区实际需求匹配
- 志愿者招募和留存本身就是运营挑战

---

## 安全与合规注意事项

### 1. 数据隐私

| 风险点 | 涉及数据 | 建议措施 |
|--------|----------|----------|
| 老人个人信息 | 姓名、地址、电话、健康状态 | 加密存储，最小化采集，明确授权 |
| 家庭关系数据 | 子女-老人绑定关系 | 双向授权确认 |
| 行为数据 | 查看记录、活动轨迹 | 明确告知用途，允许退出 |
| 就餐数据 | 食堂消费记录 | 脱敏处理，仅用于健康分析 |

**合规要求：**
- 制定并公示《隐私政策》和《用户协议》
- 遵循《个人信息保护法》相关规定
- 老人数据收集需获得监护人（子女）明确同意
- 提供数据删除和导出功能

### 2. 微信生态限制

- **小程序类目：** 需选择合适的服务类目，涉及老人健康可能需要额外资质
- **内容合规：** 小程序内不得出现医疗诊断建议，文案需从"关心"角度出发
- **用户授权：** 获取 openid 需遵循微信授权流程，不得强制收集用户信息
- **模板消息：** 发送频率和内容需符合微信平台规范，避免被判定为骚扰

### 3. 服务器部署

```
生产环境最低要求：
┌─────────────────────────────────────────────────┐
│  云服务器（推荐阿里云 ECS）                        │
│  - CPU: 2核+    内存: 4GB+    磁盘: 50GB+        │
│  - 系统: Ubuntu 22.04 LTS / CentOS 8+           │
│                                                  │
│  网络配置：                                       │
│  - 域名已完成 ICP 备案                            │
│  - SSL 证书（HTTPS 强制）                         │
│  - CDN 加速静态资源（小程序端要求 HTTPS）           │
│  - 安全组规则：仅开放 80/443 端口                  │
│                                                  │
│  数据备份：                                       │
│  - PostgreSQL 每日自动备份                         │
│  - Redis 持久化配置（AOF + RDB）                   │
│  - 备份文件异地存储                                │
└─────────────────────────────────────────────────┘
```

**Nginx 反向代理示例：**

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/ssl/certs/your-cert.pem;
    ssl_certificate_key /etc/ssl/private/your-key.pem;

    # 后端 API
    location /api/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 管理后台
    location / {
        root /var/www/admin-frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 静态文件
    location /static/ {
        alias /var/www/backend/static/;
    }
}
```

---

## 推荐落地路线图

### Phase 1：基础设施部署（1-2 周）

**目标：** 后端和管理后台上线，用模拟数据验证全流程

```
任务清单：
□ 购买云服务器，完成域名备案
□ 部署 Docker Compose 全套服务
□ 执行数据库迁移，导入种子数据
□ 配置 Nginx 反向代理 + HTTPS
□ 管理后台全流程功能测试
□ API 接口自动化测试
□ 性能基准测试（并发、响应时间）
```

**交付物：**
- 可通过域名访问的管理后台
- 健康的后端 API（health check 通过）
- 完整的模拟数据（10+ 老人、3+ 社区工作人员）

---

### Phase 2：小程序发布（2-4 周）

**目标：** 微信小程序上线，可真实体验"发送牵挂-查看内容"流程

```
任务清单：
□ 注册微信小程序账号
□ 配置微信 AppID/AppSecret
□ 后端配置微信登录和消息推送
□ 小程序构建、上传、提交审核
□ 内部体验测试（团队 + 亲友）
□ 修复审核反馈的问题
□ 正式发布
```

**交付物：**
- 微信可搜索到的小程序
- 完整的"子女发送 → 老人查看"流程可走通
- 体验版供内部测试使用

---

### Phase 3：试点社区验证（1-2 月）

**目标：** 选择 1 个试点社区，接入真实数据，验证核心机制

```
任务清单：
□ 确定试点社区，签订合作协议
□ 导入社区真实老人档案（20-50 人）
□ 培训社区工作人员使用管理后台
□ 引导老人家属注册使用小程序
□ 收集 2-4 周真实使用数据
□ 验证"已读即在线"机制有效性
□ 校准风险评分阈值
□ 收集用户反馈，迭代优化
```

**关键指标：**
- 老人查看率（日均查看次数 / 收到内容数）
- 未读检测准确率（真实未读 vs 误报）
- 社区工作人员日活使用率
- 家属满意度评分

---

### Phase 4：规模扩展（3-6 月）

**目标：** 扩展到更多社区，完善生态闭环

```
任务清单：
□ 基于试点数据优化风险评分模型
□ 接入志愿者组织，启动志愿者系统
□ 配置 AI 功能（食堂解析、文案建议）
□ 开通微信模板消息推送
□ 扩展到 3-5 个社区
□ 建立运营数据监控体系
□ 制定标准化社区接入流程
□ 探索可持续商业模式
```

**长期目标：**
- 覆盖 50+ 社区，5000+ 老人
- 风险评分模型 AUC > 0.85
- 社区工作人员日均使用时长 > 30 分钟
- 老人周活跃查看率 > 60%

---

## 附录：技术债务与改进建议

| 类别 | 项目 | 优先级 | 说明 |
|------|------|:---:|------|
| 安全 | 速率限制 | 高 | 生产环境需添加 API 请求频率限制 |
| 安全 | 日志脱敏 | 高 | 确保日志中不包含敏感个人信息 |
| 监控 | 应用监控 | 高 | 接入 APM 工具（如 SkyWalking） |
| 监控 | 告警通知 | 高 | 服务器异常时自动通知运维 |
| 测试 | 集成测试 | 中 | 补充核心业务流的自动化测试 |
| 测试 | 压力测试 | 中 | 验证多社区并发场景下的性能 |
| 运维 | CI/CD | 中 | 配置自动化构建和部署流水线 |
| 运维 | 日志收集 | 中 | 集中式日志收集和分析 |
| 功能 | 离线缓存 | 低 | 小程序端弱网环境下的数据缓存 |
| 功能 | 多语言 | 低 | 如需服务少数民族社区 |

---

> **文档版本：** v1.0
> **适用代码版本：** 当前 main 分支
> **最后更新：** 2025 年 1 月
